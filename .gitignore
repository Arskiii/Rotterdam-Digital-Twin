node_modules
dist
dist-ssr
.vite
*.local
.DS_Store
.env
.env.*
!.env.example

# raw OSM downloads (large, regenerate with npm run fetch-data)
data/raw

# raw 3D BAG pages + reduced height lookup (regenerate with npm run fetch-heights)
data/raw3dbag
data/heights-3dbag.json

# local archive slice (generated by scripts/archive-live.mjs; the real archive
# lives on the `archive` branch — never commit a synthetic one, it would be
# indistinguishable from measured history)
public/data/archive
